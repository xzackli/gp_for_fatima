[project]
name = "gp-for-fatima"
version = "0.1.0"
description = "Shared-kernel GP interpolator for multi-output surrogate modeling"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.2.0",
    "jax[cuda12]>=0.5",
    "matplotlib>=3.10",
    "numpy>=2.0",
    "scipy>=1.14",
]
